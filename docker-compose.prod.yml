# Production overlay: pull pre-built image from GHCR (see .github/workflows/deploy.yml).
# export DOCKER_IMAGE=ghcr.io/sniffy1988/iclouddl:latest
services:
  migrate:
    image: ${DOCKER_IMAGE:?Set DOCKER_IMAGE}

  api:
    image: ${DOCKER_IMAGE}

  worker:
    image: ${DOCKER_IMAGE}
